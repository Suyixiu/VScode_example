{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "compile",
            "type": "shell",
            "command": "mkdir build ; cd ${workspaceRoot}/build ; cmake .. ; make",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}

